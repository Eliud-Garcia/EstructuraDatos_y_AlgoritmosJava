#include <bits/stdc++.h>
#include <iostream>
#include <map>
using namespace std;

#define endl '\n'
#define put pair<char, int>
#define all(x) x.begin(), x.end()
#define ll long long
#define fori(iterador, inicio, fin) for (int iterador = inicio; iterador < fin; iterador++)
#define pb push_back
map<char, int> mp;

void solve()
{
	    

}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    long t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}